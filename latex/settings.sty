% **************************************************
% Packages
% **************************************************

% Use UTF-8 encoding
\usepackage[utf8]{inputenc}

% Use german language package
\usepackage[ngerman]{babel}

% Use T1 font encoding
\usepackage[T1]{fontenc}

% Reference the number of pages
\usepackage{lastpage}

% Style header and footer
\usepackage{fancyhdr}

% Embed pictures
\usepackage{graphicx}

% Embed tables
\usepackage{tabularx}

% Embed source code
\usepackage{listings}

\usepackage{algorithm}
\usepackage{algpseudocode}


% Use different colors
\usepackage{xcolor}

% Embed urls
\usepackage[urlcolor=blue, pdfborder={0 0 0}]{hyperref}

% Use multiple columns
\usepackage{multicol}

% Add list of abbreviations
\usepackage{acronym}

% Improve figure positioning
\usepackage{float}

% Use Helvetica font
\usepackage[scaled]{helvet}

% Use Times New Roman font
\usepackage{mathptmx}

% Change line spacing
\usepackage{setspace}

% Allow to draw figures
\usepackage{tikz,pgfplots}
\usepackage{tikz-network}
\usetikzlibrary{
    % external,
    arrows.meta,
    matrix,
    positioning,
    patterns,
}
% \tikzexternalize[prefix=cache/]

% Allows to draw complex tables or matrices
\usepackage{blkarray}

% Better math
\usepackage{amsmath}

% For adding captions to subfigures
\usepackage{caption}
\usepackage{subcaption}

\usepackage{tabularray}

% For proofs
\usepackage{amsthm}
\usepackage{amssymb}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}


% **************************************************
% Bibliography
% **************************************************

% Use biblatex with biber for bibliography
\usepackage[backend=biber,style=numeric,sorting=none]{biblatex}

% Add source for bibliographic database
\addbibresource{bibliography.bib}

% Provide advanced quotations
\usepackage{csquotes}


% **************************************************
% Source code formatting
% **************************************************

\renewcommand\lstlistingname{Quellcode}

\renewcommand\lstlistlistingname{Quellcodeverzeichnis}

\lstset{ %
    backgroundcolor=\color{white},
    basicstyle=\footnotesize,
    breaklines=true,
    captionpos=b,
    commentstyle=\color{green},
    escapeinside={\%*}{*)},
    keywordstyle=\color{blue},
    stringstyle=\color{purple},
    showstringspaces=false,
    showspaces=false,
    numbers=left,
    numberstyle=\tiny,
    frame=lines
}

% **************************************************
% Layout
% **************************************************
\onehalfspacing % Set line spacing of 1.5

% **************************************************
% Header and footer
% **************************************************

\pagestyle{fancy}
\fancyhf{}
\lhead{\nouppercase{\leftmark}}
\chead{}
\rhead{\thepage}
\lfoot{}
\rfoot{}


% **************************************************
% Miscellaneous
% **************************************************

% Prevent identation of new paragraphs
\setlength{\parindent}{0pt}

% Reduce hyphenation
\hyphenpenalty=5000
\tolerance=1000